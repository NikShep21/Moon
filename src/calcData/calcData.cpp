#include "calcData.h"
#include "./file/file.h"
#include "./utils/utils.h"
#include <string.h>
values calcData(const char* date){
    values result;
    return result;
    // strncpy(result.date, date, sizeof(result.date));
    // result.date[sizeof(result.date) - 1] = '\0';
    // structDate parsedDate = parseDate(date);
    // MoonData data = getDataFile(parsedDate.year, parsedDate.month, parsedDate.day);
    // return result;
}